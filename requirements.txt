# Week 1 dependencies — bounded to avoid surprise breaking changes mid-course.
# Lower bound: minimum version we rely on. Upper bound: next major (may break).
openai>=1.0.0,<2.0.0
python-dotenv>=1.0.0,<2.0.0
